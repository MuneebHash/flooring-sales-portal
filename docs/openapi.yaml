openapi: 3.0.3
info:
  title: Sales Portal MVP API
  version: 1.0.0
  description: >-
    Generated from locked Phase 4 API conventions and Chunk 1–4 contracts, plus the Phase 13
    Invoice Acceptance + Signature + Email addendum
    (docs/API-Contracts-Phase13-Acceptance-Signature-Email.md).

servers:
  - url: /api/v1/{slug}
    variables:
      slug:
        default: aussie-floors-group
        description: Business slug tenant key resolved by the backend.

security:
  - SessionCookieAuth: []

tags:
  - name: Auth
  - name: Dashboard
  - name: Orders
  - name: Customer
  - name: Addresses
  - name: DetailsOfSale
  - name: Catalog
  - name: Lines
  - name: Financials
  - name: Notes
  - name: Attachments
  - name: Invoices
  - name: Payments

paths:
  /auth/login:
    post:
      tags:
        - Auth
      summary: Login
      operationId: login
      security: []
      description: >
        Business is resolved from the URL slug. Login uses salesperson_code and password.
        On success, the server creates a server-side session cookie (no JWT). Single-store
        users receive active_store_id immediately. Multi-store users receive
        store_selection_required as true and active_store_id as null.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /auth/select-store:
    post:
      tags:
        - Auth
      summary: Select active store
      operationId: selectStore
      description: >
        Session is required, but an active store in session is not required on entry.
        The user must belong to the business resolved from slug. The requested store
        must belong to that business and the user must have access. Re-selecting the
        same store returns 200 idempotently. Selecting a different store after one is
        already selected returns 409 STORE_ALREADY_SELECTED.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SelectStoreRequest'
      responses:
        '200':
          description: Store selected or idempotent same-store reselect
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SelectStoreResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /auth/logout:
    post:
      tags:
        - Auth
      summary: Logout
      operationId: logout
      description: >
        Session is required. Active store in session is not required. The server
        invalidates the server-side session and clears the session cookie.
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NullDataResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'


  /orders:
    get:
      tags:
        - Dashboard
      summary: List dashboard orders
      operationId: listDashboardOrders
      description: >
        Standard protected endpoint. Data is scoped to the active (business_id, store_id).
        Empty results return 200 with an empty data array. Default ordering is
        sales_order.created_at descending. Search is case-insensitive partial match across
        sales_order.order_number, order_customer.first_name, order_customer.last_name,
        order_customer.email, order_customer.company_name, and order_customer.mobile.
        CANCELLED orders are included unless filtered out. No sort query parameter in MVP.
        Status filter is a single enum value only.
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/Status'
        - $ref: '#/components/parameters/WeekYear'
        - $ref: '#/components/parameters/WeekNumber'
        - $ref: '#/components/parameters/Search'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardOrdersResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

    post:
      tags:
        - Orders
      summary: Create order
      operationId: createOrder
      description: >
        Creates a new order shell. No customer, address, details-of-sale, or lines
        are required at creation time. The backend sets business_id, store_id, and
        user_id from the active session; the client never sends business_id,
        store_id, or user_id. The backend generates order_sequence_number,
        order_number, week_year, and week_number. order_status defaults to LEAD.
        supply_only defaults to false. locked is derived and false on creation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOrderRequest'
      responses:
        '201':
          description: Order created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateOrderResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}:
    get:
      tags:
        - Orders
      summary: Get order workspace
      operationId: getOrder
      description: >
        Returns the Chunk 2 workspace state: header, customer, installation
        address, billing address, details of sale, and persisted financial readback.
        This is read-only and is allowed even when order_status is LAID. The order
        must belong to the session business/store; cross-store or cross-business
        misses return 404 ORDER_NOT_FOUND. This endpoint does not include live
        order_financial_summary; that belongs to Chunk 3 mutation responses.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Order returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderWorkspaceResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/status:
    patch:
      tags:
        - Dashboard
      summary: Update order status
      operationId: updateOrderStatus
      description: >
        Dashboard-only status change on a standard protected endpoint. The order must
        belong to the session business and store. Any status can change to any other
        status. Setting LAID locks the order. Changing away from LAID unlocks the order.
        Sending the same status returns 200 as a no-op. Cross-store or cross-business
        misses return 404 ORDER_NOT_FOUND.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrderStatusUpdateRequest'
      responses:
        '200':
          description: Status updated or no-op
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderStatusUpdateResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/customer:
    put:
      tags:
        - Customer
      summary: Save order customer
      operationId: saveOrderCustomer
      description: >
        Creates or replaces the single customer row for an order. PUT is a full
        replacement for customer fields. Optional fields omitted in the request are
        written as null. There is one customer row per order. This endpoint is
        blocked when the order is LAID and returns 422 ORDER_LOCKED. The order must
        belong to the session business/store.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerUpsertRequest'
      responses:
        '200':
          description: Customer created or replaced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/addresses/installation:
    put:
      tags:
        - Addresses
      summary: Save installation address
      operationId: saveInstallationAddress
      description: >
        Creates or replaces the installation address. The path determines
        address_type = INSTALLATION and address_type is not accepted in the request
        body. PUT is a full replacement for address fields. Optional unit_number
        omitted in the request is written as null. This endpoint is blocked when the
        order is LAID and returns 422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddressUpsertRequest'
      responses:
        '200':
          description: Installation address created or replaced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InstallationAddressResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/addresses/billing:
    put:
      tags:
        - Addresses
      summary: Save billing address
      operationId: saveBillingAddress
      description: >
        Creates or replaces the billing address. The path determines
        address_type = BILLING and address_type is not accepted in the request body.
        PUT is a full replacement for address fields. Optional unit_number omitted
        in the request is written as null. This endpoint is blocked when the order
        is LAID and returns 422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddressUpsertRequest'
      responses:
        '200':
          description: Billing address created or replaced
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingAddressResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/addresses/billing/copy-from-installation:
    post:
      tags:
        - Addresses
      summary: Copy installation address to billing
      operationId: copyBillingAddressFromInstallation
      description: >
        The backend copies the existing installation address into the billing
        address. No request body is required. If no installation
        address exists, the endpoint returns 422 INSTALLATION_ADDRESS_REQUIRED. If a
        billing address exists it is replaced; if it does not exist it is created.
        Copied fields are unit_number, street_number, street, suburb, state_code,
        and postcode. This endpoint is blocked when the order is LAID and returns
        422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Billing address copied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingAddressResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/details-of-sale:
    put:
      tags:
        - DetailsOfSale
      summary: Save details of sale
      operationId: saveDetailsOfSale
      description: >
        Updates non-line, non-payment details-of-sale fields on sales_order. PUT is
        a full replacement for the fields this endpoint owns. Required fields not
        present return 400. Optional fields omitted in the request are written as
        null. This endpoint does not include order_financial_summary because these
        fields do not affect financials. Sale price override and Reset Price belong
        to Chunk 3. This endpoint is blocked when the order is LAID and returns 422
        ORDER_LOCKED. proposed_lay_date and lay_date_status must be either both
        null/omitted or both non-null.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DetailsOfSaleRequest'
      responses:
        '200':
          description: Details of sale saved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailsOfSaleResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'


  /orders/{orderId}/available-products:
    get:
      tags:
        - Catalog
      summary: List available products for order
      operationId: listAvailableProducts
      description: >
        Returns active store products available for the order's flooring type.
        Cost fields are never exposed.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/Search'
      responses:
        '200':
          description: Available products returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailableProductsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/available-charges:
    get:
      tags:
        - Catalog
      summary: List available charges for order
      operationId: listAvailableCharges
      description: >
        Returns active store charges available for the order's flooring type.
        Cost fields are never exposed.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - $ref: '#/components/parameters/Search'
      responses:
        '200':
          description: Available charges returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailableChargesResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/lines:
    get:
      tags:
        - Lines
      summary: Get order product and charge lines
      operationId: getOrderLines
      description: >
        Returns all product lines, charge lines, and live financial summary for
        the order. Per-line cost_snapshot is exposed read-only for salesperson
        display; per-line line_cost is never exposed.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Order lines returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderLinesResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/product-lines:
    post:
      tags:
        - Lines
      summary: Add product line to order
      operationId: addProductLine
      description: >
        Adds a product line and returns the updated financial summary. Backend
        snapshots product_code, product_name, pricing_unit, price, cost, and
        sqm_per_lm. Backend derives the missing quantity using the product
        line's snapshotted sqm_per_lm_snapshot and calculates line_total and
        line_cost internally. Cost fields are never
        accepted from the client. Product must belong to the active store, be
        active, and match the order flooring_type. Blocked when order is LAID
        with 422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductLineCreateRequest'
      responses:
        '201':
          description: Product line added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductLineMutationResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/product-lines/{lineId}:
    patch:
      tags:
        - Lines
      summary: Update product line quantity and/or unit price
      operationId: updateProductLine
      description: >
        Updates quantity and/or unit price for an existing product line and
        returns the updated financial summary. At least one editable field must
        be present, and at most one of quantity_lm or quantity_sqm may be
        supplied. product_id and snapshot fields cannot be changed, and cost
        fields are never accepted from the client. The line must belong to the
        order in the path. Blocked when order is LAID with 422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/LineId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductLinePatchRequest'
      responses:
        '200':
          description: Product line updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductLineMutationResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      tags:
        - Lines
      summary: Delete product line from order
      operationId: deleteProductLine
      description: >
        Deletes a product line, recomputes order_financial_summary, and returns
        the deleted line id with the updated summary. The line must belong to
        the order in the path. Blocked when order is LAID with 422
        ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/LineId'
      responses:
        '200':
          description: Product line deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductLineDeleteResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
  /orders/{orderId}/charge-lines:
    post:
      tags:
        - Lines
      summary: Add charge line to order
      operationId: addChargeLine
      description: >
        Adds a charge line and returns the updated financial summary. Backend
        snapshots charge_code, charge_name, price, and cost. Backend
        calculates line_total and line_cost internally. Cost fields are never
        accepted from the client. Charge must belong to the active store, be
        active, and match the order flooring_type. Blocked when order is LAID
        with 422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChargeLineCreateRequest'
      responses:
        '201':
          description: Charge line added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChargeLineMutationResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/charge-lines/{lineId}:
    patch:
      tags:
        - Lines
      summary: Update charge line quantity and/or unit price
      operationId: updateChargeLine
      description: >
        Updates quantity and/or unit price for an existing charge line and
        returns the updated financial summary. At least one editable field must
        be present. charge_id and snapshot fields cannot be changed, and cost
        fields are never accepted from the client. The line must belong to the
        order in the path. Blocked when order is LAID with 422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/LineId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChargeLinePatchRequest'
      responses:
        '200':
          description: Charge line updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChargeLineMutationResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'
    delete:
      tags:
        - Lines
      summary: Delete charge line from order
      operationId: deleteChargeLine
      description: >
        Deletes a charge line, recomputes order_financial_summary, and returns
        the deleted line id with the updated summary. The line must belong to
        the order in the path. Blocked when order is LAID with 422
        ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/LineId'
      responses:
        '200':
          description: Charge line deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChargeLineDeleteResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/sale-price:
    put:
      tags:
        - Financials
      summary: Override sale price
      operationId: overrideSalePrice
      description: >
        Salesperson manually overrides the GST-inclusive sale price shown in Details
        of Sale. The frontend sends the final GST-inclusive sale price only, and
        the backend derives and stores sales_order.price_adjustment_inc_gst.
        price_adjustment_inc_gst may be negative, positive, or zero. The backend
        recalculates sale_price_ex_gst, gp, and gp_percent. The frontend must not
        send derived fields. Blocked when order is LAID with 422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SalePriceOverrideRequest'
      responses:
        '200':
          description: Sale price overridden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalePriceResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/sale-price/reset:
    post:
      tags:
        - Financials
      summary: Reset sale price override
      operationId: resetSalePrice
      description: >
        Clears sales_order.price_adjustment_inc_gst back to null. Idempotent: reset
        with no current adjustment is a 200 no-op. The backend recomputes and
        returns order_financial_summary. Blocked when order is LAID with 422
        ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Sale price reset or no-op
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SalePriceResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/notes:
    get:
      tags:
        - Notes
      summary: List order notes
      operationId: listOrderNotes
      description: >
        Returns order notes in descending created_at order (newest first). Notes are
        immutable and append-only for MVP. This read endpoint is allowed even when
        the order status is LAID. The order must belong to the session business and
        store.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200':
          description: Notes returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotesResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

    post:
      tags:
        - Notes
      summary: Add order note
      operationId: addOrderNote
      description: >
        Appends a new immutable note to the order. note_text is required and must be
        non-blank after trim. The backend sets order_id, created_by_user_id, and
        created_at. The client must not send order_note_id, order_id,
        created_by_user_id, or created_at. This endpoint is allowed even when the
        order status is LAID.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddNoteRequest'
      responses:
        '201':
          description: Note added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddNoteResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/attachments:
    get:
      tags:
        - Attachments
      summary: List order attachments
      operationId: listOrderAttachments
      description: >
        Returns attachments linked to the order in descending created_at order (newest first).
        This read endpoint is allowed when the order status is LAID. The order must
        belong to the session business and store.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200':
          description: Attachments returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentsResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

    post:
      tags:
        - Attachments
      summary: Upload order attachment
      operationId: uploadOrderAttachment
      description: >
        Uploads and links an attachment to the order. Multipart upload accepts PHOTO
        only for Chunk 3. This endpoint is allowed when the order status is LAID.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/AttachmentUploadRequest'
      responses:
        '201':
          description: Attachment uploaded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/attachments/{attachmentId}:
    delete:
      tags:
        - Attachments
      summary: Delete order attachment
      operationId: deleteOrderAttachment
      description: >
        Deletes an attachment linked to the order. The attachment must belong to the
        order in the path. Deletion is blocked when the order status is LAID and
        returns 422 ORDER_LOCKED.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/AttachmentId'
      responses:
        '200':
          description: Attachment deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentDeleteResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/attachments/{attachmentId}/file:
    get:
      tags:
        - Attachments
      summary: Download order attachment file
      operationId: downloadOrderAttachmentFile
      description: >
        Returns raw binary file bytes for an attachment linked to the order. This read
        endpoint is allowed when the order status is LAID.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/AttachmentId'
      responses:
        '200':
          description: Attachment file returned
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/invoices:
    post:
      tags:
        - Invoices
      summary: Create invoice version 1
      operationId: createInvoice
      description: >
        Creates the first invoice snapshot for the order (version 1). The request body
        must be an empty JSON object. This endpoint is allowed when the order status is
        LAID if no invoice exists yet.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyObjectRequest'
      responses:
        '201':
          description: Invoice created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/invoices/rewrite:
    post:
      tags:
        - Invoices
      summary: Rewrite invoice
      operationId: rewriteInvoice
      description: >
        Creates the next official invoice version from current live order state. The
        request body must be an empty JSON object. Returns 422 when rewrite is blocked
        (including LAID order lock, missing first invoice, or failed preconditions).
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyObjectRequest'
      responses:
        '201':
          description: Invoice rewritten
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/invoices/current:
    get:
      tags:
        - Invoices
      summary: Get current invoice
      operationId: getCurrentInvoice
      description: >
        Returns the current (latest) invoice snapshot for the order. MVP exposes only the
        current invoice; invoice history and per-version detail are deferred post-MVP.
        Returns 404 when no invoice exists yet for the order.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Current invoice returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/invoices/current/file:
    get:
      tags:
        - Invoices
      summary: Download current invoice PDF file
      operationId: downloadCurrentInvoicePdf
      description: >
        Streams the current (latest) invoice PDF binary for the order. The order must belong
        to the session business/store. This read endpoint is allowed when order_status is
        LAID. Returns 404 when no invoice exists yet. The backend sets Content-Disposition
        filename as invoice-{order_number}-v{version_number}.pdf. Downloading older versions
        is deferred post-MVP.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Current invoice PDF streamed
          headers:
            Content-Disposition:
              description: Inline PDF filename generated by backend.
              schema:
                type: string
            Content-Length:
              description: PDF file size in bytes.
              schema:
                type: integer
                format: int64
                minimum: 0
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/invoices/current/accept:
    post:
      tags:
        - Invoices
      summary: Accept current invoice
      operationId: acceptCurrentInvoice
      description: >
        Accepts/signs the current invoice (Phase 13). Captures the customer signature
        (image/png, max 2 MB) and accepted customer name, appends a new signed current invoice
        version, regenerates the signed PDF, and automatically emails the accepted PDF to the
        order customer's email, updating last_emailed_at. Requires a current invoice that is not
        already accepted and a valid customer email. Allowed when order_status is LAID. If the
        email send fails the acceptance is still persisted (201) with last_emailed_at null; use
        Resend to retry.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/SignatureAcceptRequest'
            encoding:
              signature:
                contentType: image/png
      responses:
        '201':
          description: Invoice accepted (and emailed unless the send failed)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/invoices/current/resend:
    post:
      tags:
        - Invoices
      summary: Resend current accepted invoice
      operationId: resendCurrentInvoice
      description: >
        Re-sends the current accepted invoice PDF to the customer and updates last_emailed_at
        (Phase 13). The request body must be an empty JSON object. Requires a current invoice
        that has been accepted and a valid customer email. Does not require a new signature and
        does not create a new invoice version. Returns 502 when the email provider fails.
        Allowed when order_status is LAID.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmptyObjectRequest'
      responses:
        '200':
          description: Invoice re-sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '502':
          $ref: '#/components/responses/BadGateway'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/invoices/current/signature:
    get:
      tags:
        - Invoices
      summary: Download current invoice accepted signature image
      operationId: downloadCurrentInvoiceSignature
      description: >
        Streams the accepted customer signature image (image/png) for the current invoice
        (Phase 13). Returns 404 when no invoice exists yet and 422 when the current invoice has
        not been accepted. Allowed when order_status is LAID. Not an old-version
        signature-history endpoint.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      responses:
        '200':
          description: Accepted signature image streamed
          headers:
            Content-Disposition:
              description: Inline signature filename generated by backend.
              schema:
                type: string
            Content-Length:
              description: Signature file size in bytes.
              schema:
                type: integer
                format: int64
                minimum: 0
          content:
            image/png:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

  /orders/{orderId}/payments:
    get:
      tags:
        - Payments
      summary: List order payments
      operationId: listPayments
      description: >
        Returns paginated payment transactions for the order ordered by created_at
        descending, plus the current official payment summary. If no payments exist,
        payments is returned as an empty array. balance_due is null when no official
        invoice exists yet.
      parameters:
        - $ref: '#/components/parameters/OrderId'
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200':
          description: Payments returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentsResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalServerError'

    post:
      tags:
        - Payments
      summary: Record payment
      operationId: recordPayment
      description: >
        Records a payment against the latest official invoice balance and atomically
        creates the next official invoice version carrying forward sale snapshot
        fields from the latest official invoice while updating only total_paid and
        balance_due. This endpoint is allowed when order status is LAID.
        Phase 13: when the current invoice was already accepted, the new version also
        carries forward the acceptance/signature metadata and the backend attempts to
        email the updated signed PDF. That email is best-effort and NON-FATAL: if the
        email provider fails, the payment and the new invoice version still succeed and
        this endpoint still returns 201 (never 502) with last_emailed_at null and a
        message telling the user to use Resend; the payment is never rolled back because
        of an email failure. When the current invoice was not accepted, no email is sent.
      parameters:
        - $ref: '#/components/parameters/OrderId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentCreateRequest'
      responses:
        '201':
          description: >
            Payment recorded and current invoice updated. If the current invoice was accepted,
            the updated signed PDF is emailed (best-effort); on email failure the response is
            still 201 with last_emailed_at null and a message indicating Resend should be used.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentCreateResponseWrapper'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '500':
          $ref: '#/components/responses/InternalServerError'

components:
  securitySchemes:
    SessionCookieAuth:
      type: apiKey
      in: cookie
      name: JSESSIONID
      description: Server-side session cookie used by protected Sales Portal endpoints.

  parameters:
    OrderId:
      name: orderId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    LineId:
      name: lineId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    AttachmentId:
      name: attachmentId
      in: path
      required: true
      schema:
        type: integer
        format: int64
        minimum: 1
    Page:
      name: page
      in: query
      required: false
      schema:
        type: integer
        default: 1
        minimum: 1
    PageSize:
      name: page_size
      in: query
      required: false
      schema:
        type: integer
        default: 20
        minimum: 1
        maximum: 100
    Search:
      name: search
      in: query
      required: false
      schema:
        type: string
    Status:
      name: status
      in: query
      required: false
      schema:
        $ref: '#/components/schemas/OrderStatus'
    WeekYear:
      name: week_year
      in: query
      required: false
      schema:
        type: integer
        minimum: 2000
        maximum: 2100
    WeekNumber:
      name: week_number
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 53

  schemas:
    AttachmentRead:
      type: object
      additionalProperties: false
      required:
        - order_attachment_id
        - attachment_kind
        - file_name
        - mime_type
        - file_size
        - download_path
        - created_at
      properties:
        order_attachment_id:
          type: integer
          format: int64
        attachment_kind:
          $ref: '#/components/schemas/AttachmentKind'
        file_name:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
        download_path:
          type: string
        created_at:
          $ref: '#/components/schemas/Timestamp'

    AttachmentsResponse:
      type: object
      additionalProperties: false
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AttachmentRead'
        pagination:
          $ref: '#/components/schemas/Pagination'

    AttachmentUploadRequest:
      type: object
      additionalProperties: false
      required:
        - file
        - attachment_kind
      properties:
        file:
          type: string
          format: binary
        attachment_kind:
          $ref: '#/components/schemas/PhotoAttachmentKind'

    AttachmentResponse:
      type: object
      additionalProperties: false
      required:
        - attachment
      properties:
        attachment:
          $ref: '#/components/schemas/AttachmentRead'

    AttachmentResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/AttachmentResponse'
        message:
          type: string

    AttachmentDeleteResponse:
      type: object
      additionalProperties: false
      required:
        - deleted_attachment_id
      properties:
        deleted_attachment_id:
          type: integer
          format: int64

    AttachmentDeleteResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/AttachmentDeleteResponse'
        message:
          type: string

    AvailableProduct:
      type: object
      additionalProperties: false
      required:
        - product_id
        - code
        - name
        - flooring_type
        - pricing_unit
        - price
        - sqm_per_lm
        - stock_quantity
        - stock_unit
      properties:
        product_id:
          type: integer
          format: int64
        code:
          type: string
        name:
          type: string
        flooring_type:
          $ref: '#/components/schemas/FlooringType'
        pricing_unit:
          $ref: '#/components/schemas/PricingUnit'
        price:
          $ref: '#/components/schemas/Money'
        sqm_per_lm:
          type: number
          format: double
          example: 3.66
          description: >-
            Product conversion factor, SQM per 1 LM. Default 3.66; 4 m rolls may
            use 4.00.
        stock_quantity:
          type: number
          format: double
          nullable: true
        stock_unit:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/StockUnit'

    AvailableProductsResponse:
      type: object
      additionalProperties: false
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AvailableProduct'
        pagination:
          $ref: '#/components/schemas/Pagination'

    AvailableCharge:
      type: object
      additionalProperties: false
      required:
        - charge_id
        - code
        - name
        - flooring_type
        - price
      properties:
        charge_id:
          type: integer
          format: int64
        code:
          type: string
        name:
          type: string
        flooring_type:
          $ref: '#/components/schemas/FlooringType'
        price:
          $ref: '#/components/schemas/Money'

    AvailableChargesResponse:
      type: object
      additionalProperties: false
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AvailableCharge'
        pagination:
          $ref: '#/components/schemas/Pagination'

    ProductLineCreateRequest:
      type: object
      additionalProperties: false
      required:
        - product_id
      properties:
        product_id:
          type: integer
          format: int64
          minimum: 1
        quantity_lm:
          type: number
          format: double
          nullable: true
          minimum: 0
          exclusiveMinimum: true
        quantity_sqm:
          type: number
          format: double
          nullable: true
          minimum: 0
          exclusiveMinimum: true
        unit_price:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
            - type: number
              minimum: 0
              exclusiveMinimum: true
      oneOf:
        - required:
            - quantity_lm
        - required:
            - quantity_sqm
      not:
        required:
          - quantity_lm
          - quantity_sqm

    ProductLinePatchRequest:
      type: object
      additionalProperties: false
      properties:
        quantity_lm:
          type: number
          format: double
          nullable: true
          minimum: 0
          exclusiveMinimum: true
        quantity_sqm:
          type: number
          format: double
          nullable: true
          minimum: 0
          exclusiveMinimum: true
        unit_price:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
            - type: number
              minimum: 0
              exclusiveMinimum: true
      anyOf:
        - required:
            - quantity_lm
        - required:
            - quantity_sqm
        - required:
            - unit_price
      not:
        required:
          - quantity_lm
          - quantity_sqm

    ChargeLineCreateRequest:
      type: object
      additionalProperties: false
      required:
        - charge_id
        - quantity
      properties:
        charge_id:
          type: integer
          format: int64
          minimum: 1
        quantity:
          type: number
          format: double
          minimum: 0
          exclusiveMinimum: true
        unit_price:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
            - type: number
              minimum: 0
              exclusiveMinimum: true

    ChargeLinePatchRequest:
      type: object
      additionalProperties: false
      properties:
        quantity:
          type: number
          format: double
          nullable: true
          minimum: 0
          exclusiveMinimum: true
        unit_price:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
            - type: number
              minimum: 0
              exclusiveMinimum: true
      anyOf:
        - required:
            - quantity
        - required:
            - unit_price

    ChargeLineMutationResponse:
      type: object
      additionalProperties: false
      required:
        - charge_line
        - order_financial_summary
      properties:
        charge_line:
          $ref: '#/components/schemas/ChargeLineRead'
        order_financial_summary:
          $ref: '#/components/schemas/OrderFinancialSummary'

    ChargeLineMutationResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/ChargeLineMutationResponse'
        message:
          type: string

    ChargeLineDeleteResponse:
      type: object
      additionalProperties: false
      required:
        - deleted_charge_line_id
        - order_financial_summary
      properties:
        deleted_charge_line_id:
          type: integer
          format: int64
        order_financial_summary:
          $ref: '#/components/schemas/OrderFinancialSummary'

    ChargeLineDeleteResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/ChargeLineDeleteResponse'
        message:
          type: string

    ProductLineMutationResponse:
      type: object
      additionalProperties: false
      required:
        - product_line
        - order_financial_summary
      properties:
        product_line:
          $ref: '#/components/schemas/ProductLineRead'
        order_financial_summary:
          $ref: '#/components/schemas/OrderFinancialSummary'

    ProductLineMutationResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/ProductLineMutationResponse'
        message:
          type: string

    ProductLineDeleteResponse:
      type: object
      additionalProperties: false
      required:
        - deleted_product_line_id
        - order_financial_summary
      properties:
        deleted_product_line_id:
          type: integer
          format: int64
        order_financial_summary:
          $ref: '#/components/schemas/OrderFinancialSummary'

    ProductLineDeleteResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/ProductLineDeleteResponse'
        message:
          type: string


    SalePriceOverrideRequest:
      type: object
      additionalProperties: false
      required:
        - final_sale_price_inc_gst
      properties:
        final_sale_price_inc_gst:
          allOf:
            - $ref: '#/components/schemas/Money'
            - type: number
              minimum: 0
              exclusiveMinimum: true

    SalePriceResponse:
      type: object
      additionalProperties: false
      required:
        - order_financial_summary
      properties:
        order_financial_summary:
          $ref: '#/components/schemas/OrderFinancialSummary'

    SalePriceResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/SalePriceResponse'
        message:
          type: string

    ProductLineRead:
      type: object
      additionalProperties: false
      required:
        - order_product_line_id
        - product_id
        - product_code_snapshot
        - product_name_snapshot
        - pricing_unit_snapshot
        - price_snapshot
        - cost_snapshot
        - sqm_per_lm_snapshot
        - quantity_lm
        - quantity_sqm
        - unit_price
        - line_total
        - created_at
        - updated_at
      properties:
        order_product_line_id:
          type: integer
          format: int64
        product_id:
          type: integer
          format: int64
        product_code_snapshot:
          type: string
        product_name_snapshot:
          type: string
        pricing_unit_snapshot:
          $ref: '#/components/schemas/PricingUnit'
        price_snapshot:
          $ref: '#/components/schemas/Money'
        cost_snapshot:
          allOf:
            - $ref: '#/components/schemas/Money'
          description: >-
            Read-only unit cost snapshot, returned on already-added order lines
            for salesperson display. Captured from the catalog at line creation
            and immutable thereafter. The client must never send this field.
            Per-line line_cost is backend-only and is not returned.
        sqm_per_lm_snapshot:
          type: number
          format: double
          example: 3.66
          description: >-
            Read-only conversion factor snapshot (SQM per 1 LM) captured at line
            creation and immutable thereafter. The client must never send this
            field.
        quantity_lm:
          type: number
          format: double
        quantity_sqm:
          type: number
          format: double
        unit_price:
          $ref: '#/components/schemas/Money'
        line_total:
          $ref: '#/components/schemas/Money'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'

    ChargeLineRead:
      type: object
      additionalProperties: false
      required:
        - order_charge_line_id
        - charge_id
        - charge_code_snapshot
        - charge_name_snapshot
        - price_snapshot
        - cost_snapshot
        - quantity
        - unit_price
        - line_total
        - created_at
        - updated_at
      properties:
        order_charge_line_id:
          type: integer
          format: int64
        charge_id:
          type: integer
          format: int64
        charge_code_snapshot:
          type: string
        charge_name_snapshot:
          type: string
        price_snapshot:
          $ref: '#/components/schemas/Money'
        cost_snapshot:
          allOf:
            - $ref: '#/components/schemas/Money'
          description: >-
            Read-only unit cost snapshot, returned on already-added order lines
            for salesperson display. Captured from the catalog at line creation
            and immutable thereafter. The client must never send this field.
            Per-line line_cost is backend-only and is not returned.
        quantity:
          type: number
          format: double
        unit_price:
          $ref: '#/components/schemas/Money'
        line_total:
          $ref: '#/components/schemas/Money'
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'

    OrderFinancialSummary:
      type: object
      additionalProperties: false
      required:
        - product_subtotal
        - charge_subtotal
        - calculated_total_inc_gst
        - price_adjustment_inc_gst
        - final_sale_price_inc_gst
        - sale_price_ex_gst
        - total_cost
        - gp
        - gp_percent
        - gp_warning
      properties:
        product_subtotal:
          $ref: '#/components/schemas/Money'
        charge_subtotal:
          $ref: '#/components/schemas/Money'
        calculated_total_inc_gst:
          $ref: '#/components/schemas/Money'
        price_adjustment_inc_gst:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
        final_sale_price_inc_gst:
          $ref: '#/components/schemas/Money'
        sale_price_ex_gst:
          $ref: '#/components/schemas/Money'
        total_cost:
          $ref: '#/components/schemas/Money'
        gp:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
        gp_percent:
          type: number
          format: double
          nullable: true
          example: 48.57
        gp_warning:
          type: boolean

    OrderLinesResponse:
      type: object
      additionalProperties: false
      required:
        - product_lines
        - charge_lines
        - order_financial_summary
      properties:
        product_lines:
          type: array
          items:
            $ref: '#/components/schemas/ProductLineRead'
        charge_lines:
          type: array
          items:
            $ref: '#/components/schemas/ChargeLineRead'
        order_financial_summary:
          $ref: '#/components/schemas/OrderFinancialSummary'

    OrderLinesResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/OrderLinesResponse'
        message:
          type: string


    NoteRead:
      type: object
      additionalProperties: false
      required:
        - order_note_id
        - note_text
        - created_at
      properties:
        order_note_id:
          type: integer
          format: int64
        note_text:
          type: string
        created_at:
          $ref: '#/components/schemas/Timestamp'

    NotesResponse:
      type: object
      additionalProperties: false
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/NoteRead'
        pagination:
          $ref: '#/components/schemas/Pagination'

    AddNoteRequest:
      type: object
      additionalProperties: false
      required:
        - note_text
      properties:
        note_text:
          type: string

    AddNoteResponse:
      type: object
      additionalProperties: false
      required:
        - note
      properties:
        note:
          $ref: '#/components/schemas/NoteRead'

    AddNoteResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/AddNoteResponse'
        message:
          type: string

    EmptyObjectRequest:
      type: object
      additionalProperties: false

    CurrentInvoiceSummary:
      type: object
      additionalProperties: false
      required:
        - invoice_id
        - version_number
        - invoice_date
        - due_date
        - sale_price_inc_gst
        - total_paid
        - balance_due
        - created_by_user_id
        - created_at
        - pdf_download_path
        - accepted_at
        - accepted_customer_name
        - accepted_signature_present
        - accepted_signature_download_path
        - last_emailed_at
      properties:
        invoice_id:
          type: integer
          format: int64
        version_number:
          type: integer
        invoice_date:
          $ref: '#/components/schemas/Date'
        due_date:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Date'
        sale_price_inc_gst:
          $ref: '#/components/schemas/Money'
        total_paid:
          $ref: '#/components/schemas/Money'
        balance_due:
          $ref: '#/components/schemas/Money'
        created_by_user_id:
          type: integer
          format: int64
        created_at:
          $ref: '#/components/schemas/Timestamp'
        pdf_download_path:
          type: string
        accepted_at:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Timestamp'
        accepted_customer_name:
          type: string
          nullable: true
        accepted_signature_present:
          type: boolean
        accepted_signature_download_path:
          type: string
          nullable: true
        last_emailed_at:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Timestamp'

    InvoiceDetail:
      type: object
      additionalProperties: false
      required:
        - invoice_id
        - order_id
        - version_number
        - invoice_date
        - due_date
        - details_of_sale_snapshot
        - sale_price_ex_gst
        - sale_price_inc_gst
        - total_paid
        - balance_due
        - created_by_user_id
        - created_at
        - pdf_download_path
        - accepted_at
        - accepted_customer_name
        - accepted_signature_present
        - accepted_signature_download_path
        - last_emailed_at
      properties:
        invoice_id:
          type: integer
          format: int64
        order_id:
          type: integer
          format: int64
        version_number:
          type: integer
        invoice_date:
          $ref: '#/components/schemas/Date'
        due_date:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Date'
        details_of_sale_snapshot:
          type: string
        sale_price_ex_gst:
          $ref: '#/components/schemas/Money'
        sale_price_inc_gst:
          $ref: '#/components/schemas/Money'
        total_paid:
          $ref: '#/components/schemas/Money'
        balance_due:
          $ref: '#/components/schemas/Money'
        created_by_user_id:
          type: integer
          format: int64
        created_at:
          $ref: '#/components/schemas/Timestamp'
        pdf_download_path:
          type: string
        accepted_at:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Timestamp'
        accepted_customer_name:
          type: string
          nullable: true
        accepted_signature_present:
          type: boolean
        accepted_signature_download_path:
          type: string
          nullable: true
        last_emailed_at:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Timestamp'

    InvoiceResponse:
      type: object
      additionalProperties: false
      required:
        - invoice
      properties:
        invoice:
          $ref: '#/components/schemas/InvoiceDetail'

    InvoiceResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/InvoiceResponse'
        message:
          type: string

    SignatureAcceptRequest:
      type: object
      additionalProperties: false
      required:
        - accepted_customer_name
        - signature
      properties:
        accepted_customer_name:
          type: string
          minLength: 1
          maxLength: 150
          pattern: '.*\S.*'
        signature:
          type: string
          format: binary
          description: >
            Customer signature image. PNG only (image/png), non-empty, max 2 MB
            (2097152 bytes). Phase 13.


    PaymentTransactionRead:
      type: object
      additionalProperties: false
      required:
        - payment_transaction_id
        - payment_method
        - amount
        - payment_reference
        - created_at
      properties:
        payment_transaction_id:
          type: integer
          format: int64
        payment_method:
          $ref: '#/components/schemas/PaymentMethod'
        amount:
          $ref: '#/components/schemas/Money'
        payment_reference:
          type: string
          nullable: true
        created_at:
          $ref: '#/components/schemas/Timestamp'

    PaymentSummary:
      type: object
      additionalProperties: false
      required:
        - total_paid
        - balance_due
      properties:
        total_paid:
          $ref: '#/components/schemas/Money'
        balance_due:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'

    PaymentsResponse:
      type: object
      additionalProperties: false
      required:
        - payments
        - payment_summary
        - pagination
      properties:
        payments:
          type: array
          items:
            $ref: '#/components/schemas/PaymentTransactionRead'
        payment_summary:
          $ref: '#/components/schemas/PaymentSummary'
        pagination:
          $ref: '#/components/schemas/Pagination'

    PaymentsResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/PaymentsResponse'
        message:
          type: string

    PaymentCreateRequest:
      type: object
      additionalProperties: false
      required:
        - payment_method
        - amount
      properties:
        payment_method:
          $ref: '#/components/schemas/PaymentMethod'
        amount:
          allOf:
            - $ref: '#/components/schemas/Money'
            - type: number
              minimum: 0
              exclusiveMinimum: true
        payment_reference:
          type: string
          nullable: true
          minLength: 1
          pattern: '.*\S.*'

    PaymentCreateResponse:
      type: object
      additionalProperties: false
      required:
        - payment_transaction
        - payment_summary
        - current_invoice
      properties:
        payment_transaction:
          $ref: '#/components/schemas/PaymentTransactionRead'
        payment_summary:
          $ref: '#/components/schemas/PaymentSummary'
        current_invoice:
          $ref: '#/components/schemas/CurrentInvoiceSummary'

    PaymentCreateResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/PaymentCreateResponse'
        message:
          type: string

    UserSummary:
      type: object
      additionalProperties: false
      required:
        - user_id
        - first_name
        - last_name
        - salesperson_code
      properties:
        user_id:
          type: integer
          format: int64
        first_name:
          type: string
        last_name:
          type: string
        salesperson_code:
          type: string

    StoreSummary:
      type: object
      additionalProperties: false
      required:
        - store_id
        - name
        - store_code
      properties:
        store_id:
          type: integer
          format: int64
        name:
          type: string
        store_code:
          type: string

    LoginRequest:
      type: object
      additionalProperties: false
      required:
        - salesperson_code
        - password
      properties:
        salesperson_code:
          type: string
        password:
          type: string
          format: password

    LoginResponse:
      type: object
      additionalProperties: false
      required:
        - user
        - stores
        - active_store_id
        - store_selection_required
      properties:
        user:
          $ref: '#/components/schemas/UserSummary'
        stores:
          type: array
          items:
            $ref: '#/components/schemas/StoreSummary'
        active_store_id:
          type: integer
          format: int64
          nullable: true
        store_selection_required:
          type: boolean

    LoginResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/LoginResponse'
        message:
          type: string

    SelectStoreRequest:
      type: object
      additionalProperties: false
      required:
        - store_id
      properties:
        store_id:
          type: integer
          format: int64
          minimum: 1

    SelectStoreResponse:
      type: object
      additionalProperties: false
      required:
        - user
        - active_store
      properties:
        user:
          $ref: '#/components/schemas/UserSummary'
        active_store:
          $ref: '#/components/schemas/StoreSummary'

    SelectStoreResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/SelectStoreResponse'
        message:
          type: string

    DashboardCustomer:
      type: object
      additionalProperties: false
      required:
        - first_name
        - last_name
        - email
      properties:
        first_name:
          type: string
        last_name:
          type: string
        email:
          type: string

    DashboardInstallAddress:
      type: object
      additionalProperties: false
      required:
        - unit_number
        - street_number
        - street
        - suburb
        - state_code
        - postcode
      properties:
        unit_number:
          type: string
          nullable: true
        street_number:
          type: string
        street:
          type: string
        suburb:
          type: string
        state_code:
          type: string
        postcode:
          type: string

    DashboardOrderRow:
      type: object
      additionalProperties: false
      required:
        - order_id
        - order_number
        - order_sequence_number
        - flooring_type
        - order_status
        - customer
        - install_address
        - last_emailed_at
        - week_year
        - week_number
        - gp
        - invoice_accepted
      properties:
        order_id:
          type: integer
          format: int64
        order_number:
          type: string
        order_sequence_number:
          type: integer
        flooring_type:
          $ref: '#/components/schemas/FlooringType'
        order_status:
          $ref: '#/components/schemas/OrderStatus'
        customer:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/DashboardCustomer'
        install_address:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/DashboardInstallAddress'
        last_emailed_at:
          description: >
            Last date/time the order's invoice was emailed to the customer, or null if never
            emailed. Sourced from sales_order.last_emailed_at; in Phase 13 this is stamped on
            each successful invoice email (accept / resend / post-acceptance payment).
          allOf:
            - $ref: '#/components/schemas/Timestamp'
          nullable: true
        week_year:
          type: integer
        week_number:
          type: integer
        gp:
          allOf:
            - $ref: '#/components/schemas/Money'
          nullable: true
        invoice_accepted:
          type: boolean
          description: >
            True when the order's current invoice has been accepted (accepted_at set);
            false when unaccepted or no invoice exists. Phase 13. No invoice-status enum.

    DashboardOrdersResponse:
      type: object
      additionalProperties: false
      required:
        - data
        - pagination
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/DashboardOrderRow'
        pagination:
          $ref: '#/components/schemas/Pagination'

    OrderStatusUpdateRequest:
      type: object
      additionalProperties: false
      required:
        - order_status
      properties:
        order_status:
          $ref: '#/components/schemas/OrderStatus'

    OrderStatusUpdateResponse:
      type: object
      additionalProperties: false
      required:
        - order_id
        - order_number
        - order_status
        - previous_order_status
        - locked
        - updated_at
      properties:
        order_id:
          type: integer
          format: int64
        order_number:
          type: string
        order_status:
          $ref: '#/components/schemas/OrderStatus'
        previous_order_status:
          $ref: '#/components/schemas/OrderStatus'
        locked:
          type: boolean
        updated_at:
          $ref: '#/components/schemas/Timestamp'

    OrderStatusUpdateResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/OrderStatusUpdateResponse'
        message:
          type: string

    CreateOrderRequest:
      type: object
      additionalProperties: false
      required:
        - flooring_type
      properties:
        flooring_type:
          $ref: '#/components/schemas/FlooringType'

    OrderHeader:
      type: object
      additionalProperties: false
      required:
        - order_id
        - order_number
        - order_sequence_number
        - flooring_type
        - order_status
        - supply_only
        - plan_numbers
        - proposed_lay_date
        - lay_date_status
        - details_of_sale
        - last_emailed_at
        - week_year
        - week_number
        - created_at
        - updated_at
        - locked
      properties:
        order_id:
          type: integer
          format: int64
        order_number:
          type: string
        order_sequence_number:
          type: integer
        flooring_type:
          $ref: '#/components/schemas/FlooringType'
        order_status:
          $ref: '#/components/schemas/OrderStatus'
        supply_only:
          type: boolean
        plan_numbers:
          type: string
          nullable: true
        proposed_lay_date:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Date'
        lay_date_status:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/LayDateStatus'
        details_of_sale:
          type: string
          nullable: true
        last_emailed_at:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Timestamp'
        week_year:
          type: integer
        week_number:
          type: integer
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        locked:
          type: boolean

    CreateOrderResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/OrderHeader'
        message:
          type: string

    OrderCustomer:
      type: object
      additionalProperties: false
      required:
        - first_name
        - middle_name
        - last_name
        - email
        - mobile
        - home_phone
        - work_phone
        - company_name
      properties:
        first_name:
          type: string
        middle_name:
          type: string
          nullable: true
        last_name:
          type: string
        email:
          type: string
        mobile:
          type: string
        home_phone:
          type: string
          nullable: true
        work_phone:
          type: string
          nullable: true
        company_name:
          type: string
          nullable: true

    OrderAddress:
      type: object
      additionalProperties: false
      required:
        - unit_number
        - street_number
        - street
        - suburb
        - state_code
        - postcode
      properties:
        unit_number:
          type: string
          nullable: true
        street_number:
          type: string
        street:
          type: string
        suburb:
          type: string
        state_code:
          type: string
        postcode:
          type: string

    PersistedFinancials:
      type: object
      additionalProperties: false
      required:
        - sale_price_ex_gst
        - total_cost
        - gp
        - gp_percent
      properties:
        sale_price_ex_gst:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
        total_cost:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
        gp:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Money'
        gp_percent:
          type: number
          format: double
          nullable: true
          example: 48.57

    OrderWorkspace:
      type: object
      additionalProperties: false
      required:
        - order_id
        - order_number
        - order_sequence_number
        - flooring_type
        - order_status
        - supply_only
        - plan_numbers
        - proposed_lay_date
        - lay_date_status
        - details_of_sale
        - last_emailed_at
        - week_year
        - week_number
        - created_at
        - updated_at
        - locked
        - customer
        - install_address
        - billing_address
        - persisted_financials
      properties:
        order_id:
          type: integer
          format: int64
        order_number:
          type: string
        order_sequence_number:
          type: integer
        flooring_type:
          $ref: '#/components/schemas/FlooringType'
        order_status:
          $ref: '#/components/schemas/OrderStatus'
        supply_only:
          type: boolean
        plan_numbers:
          type: string
          nullable: true
        proposed_lay_date:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Date'
        lay_date_status:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/LayDateStatus'
        details_of_sale:
          type: string
          nullable: true
        last_emailed_at:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Timestamp'
        week_year:
          type: integer
        week_number:
          type: integer
        created_at:
          $ref: '#/components/schemas/Timestamp'
        updated_at:
          $ref: '#/components/schemas/Timestamp'
        locked:
          type: boolean
        customer:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/OrderCustomer'
        install_address:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/OrderAddress'
        billing_address:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/OrderAddress'
        persisted_financials:
          $ref: '#/components/schemas/PersistedFinancials'

    OrderWorkspaceResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/OrderWorkspace'

    CustomerUpsertRequest:
      type: object
      additionalProperties: false
      required:
        - first_name
        - last_name
        - email
        - mobile
      properties:
        first_name:
          type: string
        middle_name:
          type: string
          nullable: true
        last_name:
          type: string
        email:
          type: string
        mobile:
          type: string
        home_phone:
          type: string
          nullable: true
        work_phone:
          type: string
          nullable: true
        company_name:
          type: string
          nullable: true

    CustomerResponse:
      type: object
      additionalProperties: false
      required:
        - customer
      properties:
        customer:
          $ref: '#/components/schemas/OrderCustomer'

    CustomerResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/CustomerResponse'
        message:
          type: string

    AddressUpsertRequest:
      type: object
      additionalProperties: false
      required:
        - street_number
        - street
        - suburb
        - state_code
        - postcode
      properties:
        unit_number:
          type: string
          nullable: true
        street_number:
          type: string
        street:
          type: string
        suburb:
          type: string
        state_code:
          type: string
        postcode:
          type: string

    InstallationAddressResponse:
      type: object
      additionalProperties: false
      required:
        - install_address
      properties:
        install_address:
          $ref: '#/components/schemas/OrderAddress'

    InstallationAddressResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/InstallationAddressResponse'
        message:
          type: string

    BillingAddressResponse:
      type: object
      additionalProperties: false
      required:
        - billing_address
      properties:
        billing_address:
          $ref: '#/components/schemas/OrderAddress'

    BillingAddressResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/BillingAddressResponse'
        message:
          type: string

    DetailsOfSaleRequest:
      type: object
      additionalProperties: false
      description: proposed_lay_date and lay_date_status must be either both null/omitted or both non-null.
      required:
        - supply_only
      properties:
        supply_only:
          type: boolean
        plan_numbers:
          type: string
          nullable: true
        proposed_lay_date:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Date'
        lay_date_status:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/LayDateStatus'
        details_of_sale:
          type: string
          nullable: true

    DetailsOfSaleFields:
      type: object
      additionalProperties: false
      required:
        - supply_only
        - plan_numbers
        - proposed_lay_date
        - lay_date_status
        - details_of_sale
      properties:
        supply_only:
          type: boolean
        plan_numbers:
          type: string
          nullable: true
        proposed_lay_date:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Date'
        lay_date_status:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/LayDateStatus'
        details_of_sale:
          type: string
          nullable: true

    DetailsOfSaleResponse:
      type: object
      additionalProperties: false
      required:
        - details_of_sale_fields
        - updated_at
      properties:
        details_of_sale_fields:
          $ref: '#/components/schemas/DetailsOfSaleFields'
        updated_at:
          $ref: '#/components/schemas/Timestamp'

    DetailsOfSaleResponseWrapper:
      type: object
      additionalProperties: false
      required:
        - data
      properties:
        data:
          $ref: '#/components/schemas/DetailsOfSaleResponse'
        message:
          type: string

    FlooringType:
      type: string
      enum:
        - SOFT
        - HARD

    OrderStatus:
      type: string
      enum:
        - LEAD
        - NEW_ACHIEVED_SALE
        - FOLLOW_UP
        - ACCEPTED
        - LAID
        - CANCELLED

    LayDateStatus:
      type: string
      enum:
        - CONFIRMED
        - TO_BE_CONFIRMED

    PaymentMethod:
      type: string
      enum:
        - CASH
        - CREDIT_CARD
        - EFTPOS
        - BANK_TRANSFER

    AttachmentKind:
      type: string
      enum:
        - PHOTO
        - SIGNATURE

    PhotoAttachmentKind:
      type: string
      enum:
        - PHOTO

    AddressType:
      type: string
      enum:
        - BILLING
        - INSTALLATION

    PricingUnit:
      type: string
      enum:
        - LM
        - SQM

    StockUnit:
      type: string
      enum:
        - LM
        - SQM

    Money:
      type: number
      format: double
      example: 45.00
      description: AUD money amount, returned with two decimal places.

    Date:
      type: string
      format: date
      example: '2026-05-01'

    Timestamp:
      type: string
      format: date-time
      example: '2026-04-22T14:30:00'
      description: Server-local timestamp returned as YYYY-MM-DDTHH:mm:ss.

    Pagination:
      type: object
      additionalProperties: false
      required:
        - page
        - page_size
        - total_items
        - total_pages
      properties:
        page:
          type: integer
          minimum: 1
        page_size:
          type: integer
          minimum: 1
          maximum: 100
        total_items:
          type: integer
          minimum: 0
        total_pages:
          type: integer
          minimum: 0

    ValidationErrorDetail:
      type: object
      additionalProperties: false
      required:
        - message
      properties:
        section:
          type: string
          nullable: true
        field:
          type: string
          nullable: true
        message:
          type: string

    StandardError:
      type: object
      additionalProperties: false
      required:
        - code
        - message
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/ValidationErrorDetail'

    StandardErrorResponse:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        error:
          $ref: '#/components/schemas/StandardError'

    NullDataResponse:
      type: object
      additionalProperties: false
      properties:
        data:
          nullable: true
          description: Always null.
          example: null
        message:
          type: string

  responses:
    BadRequest:
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
    NotFound:
      description: Not Found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
    Conflict:
      description: Conflict
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
    UnprocessableEntity:
      description: Unprocessable Entity
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
    BadGateway:
      description: >
        Bad Gateway. An upstream/provider dependency failed (e.g. the email provider could not
        send the invoice — error.code EMAIL_SEND_FAILED). Phase 13.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
    InternalServerError:
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
